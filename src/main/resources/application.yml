spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/shopping_cart
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      port: 6379
      host: localhost